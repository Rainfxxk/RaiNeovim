require("render-markdown").setup({
    enabled = true,
    render_modes = true,
    sign = {
        enabled = false,
    },
    completions = {
        blink = {
            enabled = true,
        },
    },
})
